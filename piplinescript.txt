node {
    branchHash = sh(
        script: "echo value is: $( git ls-remote "${https://github.com/bharug3/jenkinsfile.git} && refs/heads/${env.MAIN}" | cut -f1 )",
        returnStdout: true
    ).trim()
}
