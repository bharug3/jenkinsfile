pipeline {
    agent any
    stages {
        stage('Initialization') {
            environment {
                   Hash_ID = sh (returnStdout: true, script: 'git log --format="%H" -n 1')
                  }
            steps {
                sh 'echo $Hash_ID'
                }
        }
    }
}
