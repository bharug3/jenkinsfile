pipeline {
    agent any
    stages {
        stage('Initialization') {
            environment {
                   Hash_ID = sh"git log -1"
		   Branch = sh"git branch"
                  }
            steps {
                sh 'echo $Hash_ID'
		sh 'echo $Barnch'
                }
        }
    }
}
