pipeline {
    agent any
    stages {
        stage('Initialization') {
            environment {
                   Hash_ID = sh"git log -1"
                  }
            steps {
                sh 'echo $Hash_ID'
                }
        }
    }
}
